"""Functional module boundaries for FlowMeta.

Implementation files remain in their existing locations during the
compatibility phase. Entrypoints import through these module facades so source
can be migrated incrementally without changing behavior.
"""
