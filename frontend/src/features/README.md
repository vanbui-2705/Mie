# Frontend Features

Feature packages provide stable import boundaries over the current components.
Route files remain in Next.js `app/` and implementation components remain in
their existing locations during the compatibility phase.

No UI behavior is implemented in these barrel files.

