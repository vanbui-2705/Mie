"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { toast } from "sonner"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { apiFetch } from "@/lib/api-client";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[done,setDone]=useState(false);async function submit(e:FormEvent){e.preventDefault();try{const result=await apiFetch<{message:string;reset_url?:string}>("/api/auth/forgot-password",{method:"POST",body:{email}});setDone(true);if(result.reset_url) toast.message(result.reset_url);}catch(error){toast.error(error instanceof Error?error.message:"Không thể gửi yêu cầu");}}return <main className="flex min-h-screen items-center justify-center bg-slate-950 px-4"><section className="w-full max-w-md rounded-2xl bg-white p-7 shadow-2xl"><h1 className="text-2xl font-semibold">Quên mật khẩu</h1><p className="mb-6 mt-1 text-sm text-slate-500">Nhập email để nhận liên kết đặt lại mật khẩu.</p>{done?<p className="rounded-lg bg-emerald-50 p-4 text-sm text-emerald-800">Nếu email tồn tại, hệ thống đã tạo yêu cầu đặt lại mật khẩu.</p>:<form className="grid gap-4" onSubmit={submit}><div className="grid gap-1.5"><Label htmlFor="email">Email</Label><Input id="email" type="email" value={email} onChange={(e)=>setEmail(e.target.value)} required /></div><Button className="h-11">Gửi liên kết</Button></form>}<Link href="/login" className="mt-5 block text-center text-sm font-medium text-blue-600">Quay lại đăng nhập</Link></section></main>}
